const animationStart = 0.5;
const animationShowBackground = 2;
const animationShowText = 0.5;
const animationShowCheckmark = 0.5;
const animationStop = 1.5;
const redirectMobileDelay = 1200;
const redirectDesktopDelay = 0;

export const SUBMIT_ANIMATION_TIMEOUTS = {
    animationStart,
    animationShowText,
    animationShowCheckmark,
    animationShowBackground,
    animationStop,
    redirectMobileDelay,
    redirectDesktopDelay
};

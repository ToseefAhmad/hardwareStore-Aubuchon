/**
 * This file is augmented at build time
 * using the build target src/targets/intercept/routes/buildRouteShimmerCollection.js.
 */
/**
 * @type {Map<string, function[]>}
 */
const collection = new Map();

export default collection;

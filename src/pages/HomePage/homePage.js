import React from 'react';

import SitelinksSearchBoxRichSnippet from '@app/components/SitelinksSearchBoxRichSnippet';
// `MagentoRoute` renders the CMS page, so this component only renders Sitelink Search Box structured data.
const HomePage = () => {
    return <SitelinksSearchBoxRichSnippet />;
};
export default HomePage;
